spring:
  application:
    name:
      demo4
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/students?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: cmy2665072570

mybatis:
  #mapper配置文件
  mapper-locations: classpath:Mapper/*.xml

server:
  port: 8080